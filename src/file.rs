use std::io::Result;

use png;

use crate::model::AppState;

pub fn write_png(_path: &str, _data: &AppState) -> Result<()> {
    Ok(())
}

pub fn _read_png(_path: &str, _data: &mut AppState) -> Result<()> {
    Ok(())
}
